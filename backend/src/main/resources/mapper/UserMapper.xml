<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.UserMapper">
    
    <!-- 查询所有用户 -->
    <select id="findAll" resultType="User">
        SELECT id, username, password, nickname, email, phone, avatar, role, status, create_time, update_time
        FROM user
        ORDER BY create_time DESC
    </select>
    
    <!-- 根据ID查询用户 -->
    <select id="findById" resultType="User">
        SELECT id, username, password, nickname, email, phone, avatar, role, status, create_time, update_time
        FROM user
        WHERE id = #{id}
    </select>
    
    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultType="User">
        SELECT id, username, password, nickname, email, phone, avatar, role, status, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>
    
    <!-- 新增用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, email, phone, avatar, role, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{avatar}, #{role}, #{status}, NOW(), NOW())
    </insert>
    
    <!-- 更新用户 -->
    <update id="update">
        UPDATE user
        SET nickname = #{nickname}, email = #{email}, phone = #{phone}, 
            avatar = #{avatar}, role = #{role}, update_time = NOW()
        WHERE id = #{id}
    </update>
    
    <!-- 删除用户 -->
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
    
    <!-- 更新用户状态 -->
    <update id="updateStatus">
        UPDATE user SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>
    
    <!-- 统计用户总数 -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM user
    </select>
</mapper>
